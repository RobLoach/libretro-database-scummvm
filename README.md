# libretro-scummvm.dat

Builds ScummVM.dat for libretro.

## Build

1. Ensure you have ScummVM installed at either `scummvm` or `flatpak run org.scummvm.ScummVM`
1. Put `ScummVM.xml`, `ScummVM SVN.xml` in the root
1. `npm install`
1. `npm test`
